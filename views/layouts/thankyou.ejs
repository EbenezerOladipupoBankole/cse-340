<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { %>
        <script>
            window.location.href = '/';
        </script>
        <% } %>

            <%- messages() %>

                <div class="account-management-view">
                    <p>We have received your message and will get back to you shortly.</p>
                    <p><a href="/" class="contact-button">Return to Home</a></p>
                </div>